# CSE644 Lab — Terraform: GKE Autopilot (Phase 5 — not started)

Planned extension: managed GKE Autopilot cluster, for comparison against
the self-managed k3s builds (single-VM and multi-VM). Useful for
understanding what a managed control plane abstracts away, but note this
falls outside the "local Kubernetes environment" the CSE644 assignments
require — it's a personal-learning extension, not part of the graded
submission.

Not built yet.

Independent from `terraform/single-vm/` and `terraform/multi-vm/` — own
state, own lifecycle, applied separately.
